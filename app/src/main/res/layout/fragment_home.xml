<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/margin">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="@dimen/margin_medium"
        android:text="KỲ THI THPT QUỐC GIA 2018"
        android:textColor="@color/red"
        android:textSize="23sp"
        android:textStyle="bold" />

    <cn.iwgang.countdownview.CountdownView
        android:id="@+id/countdownView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="@dimen/margin"
        app:isHideTimeBackground="false"
        app:isShowDay="true"
        app:isShowHour="true"
        app:isShowMinute="true"
        app:isShowSecond="false"
        app:suffixDay="Ngày"
        app:suffixGravity="center"
        app:suffixHour="Giờ"
        app:suffixLRMargin="@dimen/margin_medium"
        app:suffixMinute="Phút"
        app:suffixTextColor="@color/red"
        app:suffixTextSize="@dimen/text_size_title"
        app:timeBgColor="@color/green"
        app:timeBgRadius="5dp"
        app:timeTextColor="@color/white"
        app:timeTextSize="28sp" />

    <GridView
        android:id="@+id/gridSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:numColumns="3"></GridView>
</LinearLayout>